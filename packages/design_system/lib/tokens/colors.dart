import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary       = Color(0xFF4F46E5);
  static const primaryLight  = Color(0xFF6366F1);
  static const primaryDark   = Color(0xFF3730A3);
  static const primarySurface = Color(0xFFEEF2FF);

  // Accent
  static const accent   = Color(0xFFF59E0B);
  static const accentBg = Color(0xFFFFFBEB);

  // Semantic
  static const success = Color(0xFF10B981);
  static const danger  = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);
  static const info    = Color(0xFF3B82F6);

  // Neutrals - Light
  static const background = Color(0xFFF8FAFC);
  static const surface    = Colors.white;
  static const border     = Color(0xFFE2E8F0);
  static const divider    = Color(0xFFF1F5F9);

  // Text - Light
  static const textPrimary   = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF475569);
  static const textMuted     = Color(0xFF94A3B8);
  static const textOnPrimary = Colors.white;

  // Neutrals - Dark
  static const backgroundDark = Color(0xFF0F172A);
  static const surfaceDark    = Color(0xFF1E293B);
  static const borderDark     = Color(0xFF334155);
  static const dividerDark    = Color(0xFF1E293B);

  // Text - Dark
  static const textPrimaryDark   = Color(0xFFF8FAFC);
  static const textSecondaryDark = Color(0xFFCBD5E1);
  static const textMutedDark     = Color(0xFF64748B);

  // Social
  static const like   = Color(0xFFEF4444);
  static const save   = Color(0xFF4F46E5);
  static const follow = Color(0xFF10B981);

  // Badges
  static const badgeGold   = Color(0xFFF59E0B);
  static const badgeSilver = Color(0xFF94A3B8);
  static const badgeBronze = Color(0xFFB45309);
  static const verified    = Color(0xFF3B82F6);

  // Extended semantic (used by admin + new screens)
  static const suspended   = Color(0xFFDC2626);
}
